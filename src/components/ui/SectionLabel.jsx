const SectionLabel = ({ children }) => (
  <div style={{
    fontSize: 15,
    fontWeight: 700,
    color: "#101828",
    marginBottom: 12,
    letterSpacing: "-0.01em",
  }}>
    {children}
  </div>
);

export default SectionLabel;
